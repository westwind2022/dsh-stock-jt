# 坐标特征表（在哪 · 实坐标）

**日期：** 2026-08-21
**状态：** P1 骨架阶段组装（三流派坐标特征归位）
**角色：** 坐标（在哪）——已实现的结构位置，可确定读取。
**输入：** 三流派自声明卡（缠论卡 A / 波浪卡 B / 威科夫卡 C）+ 概念词典。

> 本文件按**角色**组织，内部按**流派**分节。每特征已含「流派/角色」由文件与分节确定，表中补「特征名 + 契合闸门 + 锚字段 + 阈值/口径 + 执行方」。

## 缠论（坐标主力）

| 特征名 | 契合闸门 | 锚字段 | 阈值 / 口径 | 执行方 |
| :--- | :--- | :--- | :--- | :--- |
| 笔定位 | 1/2/3 | `bi_frac.frac_type` / `frac_idx` / `frac_price` + `prev1~3`/`next1~2` | 笔分型双向链表，prev2=前一同向笔（背驰比较） | 机器 |
| 段定位 | 1/2/3 | `duan_frac.frac_idx` 穿透 `bi_frac` | 段无量化列，经 `frac_idx` 平移取笔量化 | 机器 |
| 路定位 | 1/2/3 | `road_frac.frac_idx` 穿透 `bi_frac` | 路 ⊆ 段 ⊆ 笔 | 机器 |
| 中枢定位 | 2 | `zhongshu.zg`/`zd`（区间）+ `gg`/`dd`（震荡极值）+ `zs_start~end_idx`（范围） | 区间 vs 范围 vs 震荡极值三语义区分 | 机器 |

## 波浪（坐标次要）

| 特征名 | 契合闸门 | 锚字段 | 阈值 / 口径 | 执行方 |
| :--- | :--- | :--- | :--- | :--- |
| 前腿 / 后腿 / 第三腿（`nShapeLeg`） | 1/2/3 | `bi_frac.frac_type` + `prev1/prev2_frac_idx` | 前腿=向上笔、后腿=向下笔、第三腿=架梯完成后的向上突破笔 | 机器 |
| 腿—浪映射（`waveOrder`） | 全部 | 闸门位置推导（无需新字段） | 前腿=1浪、后腿=2浪、第三腿=3浪，**不判浪序** | 机器 |
| 两线三点 C 点双重突破（`doubleBreakout`） | 1 | `bi_frac.frac_price` 突破前高 + `kline.ma60` 上翘 | C 点 = 同时破反压线 L1 + 水平压力线 L2 ≈ 成坡 | 机器 |

## 威科夫（坐标次要）

| 特征名 | 契合闸门 | 锚字段 | 阈值 / 口径 | 执行方 |
| :--- | :--- | :--- | :--- | :--- |
| 交易区间 / 箱体（`creekUpper` / `rangeLower`） | 2 | `zhongshu.zg`（Creek 上沿）/ `zhongshu.zd`（区间下沿） | 吸筹控盘区 / 派发区上下沿 | 机器 |
