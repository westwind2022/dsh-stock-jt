# 刻度特征表（会到哪 · 何时 · 虚坐标）

**日期：** 2026-08-21
**状态：** P1 骨架阶段组装（三流派刻度特征归位）
**角色：** 刻度（会到哪 / 何时）——由已实现坐标**几何投射**出的待观察价格 / 时间位置。
**输入：** 三流派自声明卡 + 概念词典。

> 刻度是**几何投射事实**（可复算、不含判断），非预测。「投射 ≠ 预测」：机器算刻度，人观察刻度被到达后的表现。**目标位清单不入极性表**（是价格清单，交付给人）。

## 缠论（刻度次要）

| 特征名 | 契合闸门 | 锚字段 | 阈值 / 口径 | 执行方 |
| :--- | :--- | :--- | :--- | :--- |
| 回调比例 | 2 | `bi_frac.pct_change`（负向笔幅度） | 0.382 浅 / 0.5 / 0.618 深 | 机器 |
| 背驰面积比 | 1/失败 | `bi_frac.macd_area` 后段 ÷ 前段 | 后段 < 前段 = 背驰（比较关系） | 机器 |

## 波浪（刻度主力）

| 特征名 | 契合闸门 | 锚字段 | 阈值 / 口径 | 执行方 |
| :--- | :--- | :--- | :--- | :--- |
| 回撤分位（`fibRetraceZone`） | 2 | 三点自算（前腿高低点 + 后腿低点） | 回调幅度 ÷ 前腿幅度；0.382/0.5/0.618 | 机器 |
| 波浪尺六档（`waveRuler_targets[k]`） | 3 触发侧 | 后腿低点 + k×(前腿高−前腿低) | k ∈ {1, 1.382, 1.5, 1.618, 2, 2.618} | 机器算 + 人用 |
| 上一轮 3浪/1浪 实测比（`waveRuler_prevCycle3to1`） | 1 | 上一轮三点自算 | ≥1 为强（成/败先行指标） | 机器 |
| 时间周期（`waveTimeRatio`） | 2 | `bi_frac.frac_idx` 差值 | 平衡 t2=t1 / 叠加 t3=t1+t2 / 同构 t3=t1 | 机器 |
| 时间失效判据（`waveTimeValid`） | 2 | 同上 | t2<1/2×t1 或 t3 明显<1/2(t1+t2) → 不可靠 | 机器 |
| 四方图共振区（`resonanceZone`） | 2/卖点 | 多周期分片交叉 | 大周期黄金分割点 + 小周期波浪尺幅度 → 时空支撑/压力区 | 机器投射 + 人观察 |

## 威科夫（刻度次要）

| 特征名 | 契合闸门 | 锚字段 | 阈值 / 口径 | 执行方 |
| :--- | :--- | :--- | :--- | :--- |
| 量比（`volRatio`） | 全部 | `bi_frac.vol_sum` ÷ `kline.vol_ma20` | 放量 / 缩量客观倍数 | 机器 |
| 时间跨度（`zsTimeSpan`） | 2 | `zhongshu.zs_end_idx` − `zs_start_idx` | >20（理想 >40） | 机器 |
