# 成色特征表（多强 · 真不真）

**日期：** 2026-08-21
**状态：** P1 骨架阶段组装（三流派成色特征归位）
**角色：** 成色（多强 / 真不真）——已实现位置上的力度 / 供需评价，三流派并重。
**输入：** 三流派自声明卡 + 概念词典。

> 成色是三流派**并重**的角色（缠论背驰力度 + 波浪均线多头 + 威科夫供需）。执行方标「人」的特征照样入卡，只不进机器实现清单。

## 缠论（成色主力之一）

| 特征名 | 契合闸门 | 锚字段 | 阈值 / 口径 | 执行方 |
| :--- | :--- | :--- | :--- | :--- |
| MACD 背驰 | 1/失败 | `bi_frac.macd_area` 后段 vs `prev2_frac_idx` 前段 | 后段 < 前段 = 背驰 | 机器 |
| 力度背驰 | 1/失败 | `bi_frac.pct_change` 后段 vs 前段 | 后段绝对值 < 前段 | 机器 |
| 量能（堆量/缩量） | 1/2 | `bi_frac.vol_sum` + `kline.vol_ma20` | 进攻放量 ×1.2 / 回调缩量 ×0.7 | 机器 |
| 中枢质量（窄幅） | 2 | `zhongshu.range_ratio` / `oscillation_ratio` | range_ratio < 0.1 窄幅 | 机器 |
| 突破真假 | 3 | `bi_frac.break_type` | `breakout_valid` 真突破 / `breakout_fake` 假突破 | 机器 |

## 波浪（成色次要）

| 特征名 | 契合闸门 | 锚字段 | 阈值 / 口径 | 执行方 |
| :--- | :--- | :--- | :--- | :--- |
| 均线多头排列 | 1 | `kline.ma5` > `ma20` > `ma60` | 多头排列 = 前腿有力度 | 机器 |
| 回调深浅 | 2 | `fibRetraceZone`（后腿幅度 ÷ 前腿幅度） | 0.382 浅=强（表强）/ 0.5 / 0.618 深=弱 | 机器 |

## 威科夫（成色主力之一）

| 特征名 | 契合闸门 | 锚字段 | 阈值 / 口径（重建自 `FeatureSpec.h`） | 执行方 |
| :--- | :--- | :--- | :--- | :--- |
| 供给枯竭（`supplyExhaustion`） | 2 | `bi_frac.vol_sum` 沿 prev 链递减 | 每次回区间底部量一次比一次低 | 机器 |
| 恐慌抛售放量（`zsPsVolExpand`） | 2 | `bi_frac.vol_sum` ÷ `kline.vol_ma20` | >2 倍（理想 >3） | 机器 |
| 自然反弹缩量（`zsArVolShrink`） | 2 | 后一笔 `vol_sum` ÷ 前一笔 `vol_sum` | <0.7（理想 <0.5） | 机器 |
| 上涨笔均量扩张（`zsUpLegAvgVolExpand`） | 2 | `vol_sum` 按 `frac_type` 分组比 | >1.2（理想 >1.5） | 机器 |
| 下跌笔均量收缩（`zsDownLegAvgVolShrink`） | 2 | 同上 | <0.8（理想 <0.6） | 机器 |
| 洗盘 vs 派发（`washoutOrDistribution`） | 2 | `bi_frac.vol_sum` 趋势 + `zhongshu.range_ratio` | 缩量收敛=洗盘、放量=派发 | 机器 |
| 弹簧 / 终极震仓（`springTso`） | 2 | `bi_frac.break_type`（`breakdown_fake`）+ `vol_sum` 缩量 | 假跌破区间底部 + 缩量速收 | 机器 |
| 窄幅（`zsNarRange`） | 2 | `zhongshu.range_ratio` | <0.1（供应/需求快耗尽） | 机器 |
| 努力无结果（`sot`） | 失败/卖点 | `bi_frac.vol_sum`↑ 而 `pct_change`↓ | 放量滞涨 = 停止行为 | 人 |
| 突破递减（`breakoutDecay`） | 顶部 | `bi_frac.pct_change` 沿链递减 | 顶部突破幅度一次比一次弱 | 机器观察 + 人用 |
